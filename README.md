# One Million Checkboxes (Simplified)

A real-time checkbox grid powered by **WebSockets + Redis + PostgreSQL + OIDC (Google login)**.

---

## Features

* Real-time checkbox sync (WebSocket)
* Google login (OIDC)
* Redis for fast state handling
* PostgreSQL for user data
* Clean minimal UI

